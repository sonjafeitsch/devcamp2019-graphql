# type Query {
#   info: String!
#   users: [User!]!
#   user(id: ID!): User
# }

# type Mutation {
#   createUser(name: String!): User!
# }

type User {
  id: ID! @unique,
  name: String!,
  createdAt: DateTime!
}